# Generated by DeepAgent — do not use as service ground truth.
services:
  app:
    build: .
    image: app:local
    ports:
      - "0:0"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:0/"]
      interval: 30s
      timeout: 3s
      retries: 3
