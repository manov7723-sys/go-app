apiVersion: v1
kind: Service
metadata:
  name: go-app
  namespace: t5
  labels:
    app.kubernetes.io/name: go-app
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: go-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
